(deflisten ZA "")
(defwidget Alert []
    (label :text {zombie_status == "ok" ? "" : zombie_status})
)

(defwindow alert
  :monitor 0
  :windowtype "dock"
  :geometry 
    (
      geometry 
      :x "0%"
      :y "0.5%"
      :width "99.6%"
      :height "30px"
      :anchor "top center"
    )
  :reserve 
  (struts :side "bottom" :distance "4%")
  (Alert)
)